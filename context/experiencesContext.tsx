import {createContext} from "react";

const experiencesContext = createContext({

    sumPickusGlobal:0,
    sumPresenceGlobal:0,
    personPickusGlobal:0,
    personPresenceGlobal:0,

} ) 

export default experiencesContext;